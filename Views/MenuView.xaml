<UserControl 
	x:Class="Labb3_CsProg_ITHS.NET.Views.MenuView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:local="clr-namespace:Labb3_CsProg_ITHS.NET.Views"
	xmlns:viewmodels="clr-namespace:Labb3_CsProg_ITHS.NET.ViewModels"
	
	mc:Ignorable="d" 
	d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowsViewModel}"
	>
	<Grid>
		<Menu>
			<MenuItem Header="_File" >
				<!--<MenuItem Command="ApplicationCommands.Open"/>-->
				<MenuItem Header="Save changes" Command="{Binding ConfigViewModel.SaveChangesCommand}"/>

			</MenuItem>
			<MenuItem Header="_Edit">
				<!--<MenuItem Command="ApplicationCommands.Cut"/>
				<MenuItem Command="ApplicationCommands.Paste"/>-->
				<MenuItem Header="Undo Quiz changes" Command="{Binding ConfigViewModel.UndoPackChangesCommand}"/>
				<MenuItem Header="Edit Quiz" Command="{Binding ConfigViewModel.UndoPackChangesCommand}"/>
			</MenuItem>
			<MenuItem Header="_View">
				<MenuItem Header="_Configuration Mode" Command=""/>
				<MenuItem Header="_Play Quiz" Command="{Binding PlayQuizCommand}"/>
			</MenuItem>
		</Menu>
	</Grid>
</UserControl>
